# =============================================================================
# AXIAL BACKEND - PRODUCTION REQUIREMENTS
# =============================================================================
# Last Updated: 2026-02-02
# All versions verified for mutual compatibility and security compliance
# Security audit: Aikido Security Scan compliance
# =============================================================================

# --- CORE FRAMEWORK ---
# FastAPI 0.115.0+ includes starlette>=0.40.0 (CVE-2024-47874 fix)
# Starlette 0.47.2+ fixes CVE-2025-54121 (multipart DoS)
fastapi>=0.115.0
starlette>=0.47.2
uvicorn>=0.30.0

# --- DATABASE & ORM ---
# SQLAlchemy 2.0.30+ for Python 3.11+ compatibility
sqlalchemy>=2.0.30
alembic>=1.13.1
sqlmodel==0.0.16

# --- PYDANTIC ---
# Pydantic 2.11.7+ required by realtime/supabase 2.27.x
pydantic>=2.11.7
pydantic-settings>=2.1.0

# --- HTTP CLIENT ---
# httpx 0.27+ works with supabase 2.27.x ecosystem
# Verified compatible: httpx 0.28.1 + supabase 2.27.2
httpx[http2]>=0.27.0

# --- SUPABASE ECOSYSTEM ---
# Supabase 2.11.0+ supports httpx 0.27.x
supabase>=2.11.0,<3.0.0
# PostgreSQL drivers: psycopg3 preferred, psycopg2 as fallback
psycopg[binary]>=3.1.0
psycopg2-binary>=2.9.9  # Fallback if psycopg3 fails to install

# --- AUTHENTICATION ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart>=0.0.18  # CVE-2024-53981 DoS fix

# --- REDIS & CELERY ---
redis==5.0.1
celery[redis]==5.3.6

# --- AI & LANGCHAIN ECOSYSTEM ---
# CRITICAL: All langchain packages must be 0.3.x+ for langchain-core>=0.3.81
# Security fixes: SQL injection, SSRF, XXE, RCE, Template injection
openai>=1.12.0,<2.0.0
langchain>=0.3.0
langchain-core>=0.3.81
langchain-openai>=0.3.0
langchain-community>=0.3.27
langchain-groq>=0.3.0
langchain-text-splitters>=0.3.0  # MUST be 0.3.x for langchain-core 0.3.x compatibility
groq
tiktoken>=0.5.2

# --- LANGCHAIN DEPENDENCIES ---
# Pin these to avoid conflicts
tenacity>=8.5.0
numpy>=1.24.0
packaging>=23.2

# --- DOCUMENT PROCESSING ---
python-docx==1.1.0
pypdf>=6.6.2           # CVE-2025-62708, CVE-2025-62707, CVE-2025-55197 + 5 more
PyMuPDF>=1.26.7        # AIKIDO-2025-10959 path traversal
beautifulsoup4==4.12.3
extract-msg>=0.45.0,<1.0.0
openpyxl>=3.1.0,<4.0.0
pandas>=2.0.0,<3.0.0
python-pptx>=0.6.21,<1.0.0
unstructured>=0.16.20  # XXE and resource exposure (5 CVEs)
pdfminer.six==20231228
docx2txt==0.8
markdown==3.5.2

# --- FILE HANDLING ---
python-magic==0.4.27
lxml

# --- EMAIL & VALIDATION ---
email-validator==2.1.0.post1

# --- GOOGLE SERVICES ---
google-api-python-client
google-auth-oauthlib
google-auth-httplib2

# --- SECURITY & CRYPTO ---
cryptography

# --- WEB UTILITIES ---
jinja2
slowapi
trafilatura
ultimate-sitemap-parser
youtube-transcript-api

# --- MALWARE SCANNING ---
clamd>=1.0.2,<2.0.0

# --- SFTP/SSH ---
paramiko>=3.4.0,<4.0.0

# --- AWS S3 ---
boto3>=1.34.0,<2.0.0
botocore>=1.34.0,<2.0.0

# --- OCR ---
pytesseract>=0.3.10
pdf2image>=1.16.0
Pillow>=10.3.0         # CVE-2023-4863 (RCE), CVE-2023-50447, CVE-2024-28219

# --- MONITORING ---
psutil>=5.9.0
prometheus-client==0.20.0

# --- ADVANCED DOCUMENT PARSING ---
llama-parse>=0.5.0
nest_asyncio

# --- HTTP REQUESTS ---
requests>=2.32.4       # CVE-2024-35195, CVE-2024-47081 credential leaks
resend>=2.0.0          # Required for requests>=2.32.4 compatibility

# --- OBSERVABILITY ---
sentry-sdk[fastapi]>=2.35.0

# --- ASYNC ---
async-lru==2.0.4
